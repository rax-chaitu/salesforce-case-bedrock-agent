# ============================================================================
# THIS FILE IS NOT USED - FOR REFERENCE ONLY
# ============================================================================
# 
# Configuration is managed via:
# 1. terraform/terraform.tfvars - Infrastructure config (copy from .example)
# 2. AWS Secrets Manager - Private key storage
#
# See README.md for setup instructions
# ============================================================================

# AWS (set via: eval $(aws configure export-credentials --profile YOUR_PROFILE --format env))
# AWS_PROFILE=sandbox4

# All other config is in terraform/terraform.tfvars
